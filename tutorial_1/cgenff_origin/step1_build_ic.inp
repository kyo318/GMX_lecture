* Reads the IPEN residue from the topology file and writes a pdb consisting
* of a single IPEN molecule
*

! Read topology and parameter files
stream toppar.str

stream toppar_my_molecule.str
! Generate segment
read sequence CABZ 1
generate L1 first none last none setup warn

! Generate internal coordinates
ic generate
ic param all
ic print

! Generate cartesian coordinates
ic seed	 L1 1 C1  L1 1 C2  L1 1 C3
ic build

! Write coordinates to PDB
write coor pdb name L1_build.pdb



